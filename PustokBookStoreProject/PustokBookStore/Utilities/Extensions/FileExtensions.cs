namespace PustokBookStore.Utilities.Extensions
{
    static public class FileExtensions
    {


        //public static bool CheckFileType(this IFormFile)
        //public static async Task<string> CreateFile(this IFormFile file, string root, params string[] folders)
        //{

        //    string fileName = Guid.NewGuid().ToString() + file.FileName;
        //    string path = root;
        //    for (int i = 0; i < folders.Length; i++)
        //    {
        //        path = Path.Combine(path, folders[i]);
        //    }
        //    path = Path.Combine(path, fileName);
        //}
    }
}
